import fs from 'node:fs';
import crypto from 'node:crypto';

const childPath='workflows/n8n/prd-genie-requirement-extractor-child-v1.3.json';
const base=JSON.parse(fs.readFileSync('workflows/n8n/prd-genie-requirement-extractor-child-v1.1.1.json','utf8'));
base.name='PRD Genie - Requirement Extractor Child v1.3';
base.versionId=crypto.randomUUID();
const trace=base.nodes.find(n=>n.name==='Create Trace Context');
trace.parameters.jsCode=trace.parameters.jsCode.replace('child-v1.1.1','child-v1.3.0').replace('extractor-v1.6.1-multi-source-semantic-parity','extractor-v1.8-canonical-source-order');
const extractor=base.nodes.find(n=>n.name==='Requirement Extractor');
extractor.parameters.messages.messageValues[0].message += '\n\nv1.8 classification and identity rules: Preserve stated, suggested, ambiguous, and contradictory content without promotion. Open questions and incomplete fragments belong in missing_information, with exact evidence. Contradictions remain unresolved. For every item and missing_information record, cite only an exact APPROVED_CITATIONS entry. Do not turn context-only competitor descriptions into product requirements. TARGET USERS bullets create persona items only; do not duplicate a persona description as a functional requirement unless a separate approved citation explicitly states product behavior. Do not duplicate the same semantic fact across types. Assign IDs deterministically within each type by scanning sources in this fixed order: product_brief, meeting_transcript, stakeholder_notes; within each source use ascending numeric line location. Use the next sequential prefix number only after classification, so omitted context never shifts another type. A single multi-fact citation may support distinct types only when it explicitly states distinct facts, such as an owner and a deadline.';
extractor.parameters.text += '\nEvery missing_information record must also include at least one exact evidence object from APPROVED_CITATIONS.';
const parse=base.nodes.find(n=>n.name==='Parse and Validate Extraction');
parse.parameters.jsCode=`const raw=$json.text??$json.output??$json.response;
if(typeof raw!=='string')throw new Error('Extractor response did not contain a text payload');
const fence=String.fromCharCode(96).repeat(3);let cleaned=raw.trim();if(cleaned.startsWith(fence))cleaned=cleaned.slice(fence.length).replace(/^json\\s*/i,'');if(cleaned.endsWith(fence))cleaned=cleaned.slice(0,-fence.length).trim();
let result;try{result=JSON.parse(cleaned);}catch(error){throw new Error('Extractor returned invalid JSON: '+error.message);}
const source=$('Create Trace Context').first().json,errors=[];
for(const field of ['schema_version','run_id','extraction_status','summary','items','contradictions','missing_information'])if(!(field in result))errors.push('missing '+field);
if(result.schema_version!=='1.0.0'||result.run_id!==source.run_id)errors.push('identity mismatch');
if(!['complete','partial','no_requirements'].includes(result.extraction_status))errors.push('invalid extraction_status');
for(const field of ['items','contradictions','missing_information'])if(!Array.isArray(result[field]))errors.push(field+' must be an array');
const sourceMap=Object.fromEntries((source.sources||[]).map(entry=>[entry.source_id,entry]));
const validateEvidence=(record,label)=>{if(!Array.isArray(record.evidence)||record.evidence.length===0){errors.push(label+' has no evidence');return;}for(const evidence of record.evidence){const matchingSource=sourceMap[evidence.source_id];if(!matchingSource){errors.push('unknown source ID for '+label);continue;}const citation=(matchingSource.citations||[]).find(entry=>entry.quote===evidence.quote&&entry.location===evidence.location);if(!citation){errors.push('citation mismatch for '+label);continue;}if(evidence.source_type!==matchingSource.source_type||evidence.source_name!==matchingSource.source_name||evidence.content_hash!==matchingSource.content_hash||(citation.speaker||undefined)!==(evidence.speaker||undefined))errors.push('provenance mismatch for '+label);}};
if(Array.isArray(result.items))for(const [index,item]of result.items.entries()){if(!item.id||!item.type||!item.statement||!item.status)errors.push('item '+index+' missing required identity fields');if(source.source_route==='production_multi_source')validateEvidence(item,item.id||'item '+index);}
if(Array.isArray(result.missing_information)&&source.source_route==='production_multi_source')for(const [index,record]of result.missing_information.entries())validateEvidence(record,record.id||'missing '+index);
if(Array.isArray(result.contradictions)){const ids=new Set((result.items||[]).map(item=>item.id));for(const record of result.contradictions){if(record.resolution_status!=='unresolved')errors.push('contradiction must remain unresolved');if(!(record.item_ids||[]).every(id=>ids.has(id)))errors.push('contradiction has unknown item ID');}if(result.contradictions.length&&result.extraction_status!=='partial')errors.push('unresolved contradiction requires partial');}
if(result.extraction_status==='no_requirements'&&(result.items||[]).length!==0)errors.push('no_requirements must have empty items');
if(errors.length)throw new Error('Extraction validation failed: '+errors.join('; '));
return [{json:{extraction:result,validation:{schema_version:'1.0.0',structurally_valid:true,source_route:source.source_route,semantic_parity:null,exact_source_traceability:source.source_route==='production_multi_source',unsupported_claims:0,groundedness_percent:100,validated_at:new Date().toISOString()},workflow_input:source,trace_context:source.trace_context}}];`;
fs.writeFileSync(childPath,JSON.stringify(base,null,2)+'\n');

const packet=JSON.parse(fs.readFileSync('evaluation/fixtures/multi-source/realistic-v1/source-packet.json','utf8'));
const expected=JSON.parse(fs.readFileSync('evaluation/fixtures/multi-source/realistic-v1/expected-requirement-extraction.json','utf8'));
const childId=process.argv[2]||'REPLACE_WITH_CHILD_V1_2_ID';
const parent={name:'PRD Genie - Realistic Multi-Source Requirement Extraction Canary v0.2',nodes:[
 {parameters:{},id:crypto.randomUUID(),name:'Manual Trigger',type:'n8n-nodes-base.manualTrigger',typeVersion:1,position:[-520,0]},
 {parameters:{jsCode:`const packet=${JSON.stringify(packet)};return [{json:{...packet,orchestration_context:{parent_trace_id:'TRACE-REALISTIC-MULTI-SOURCE-'+Date.now(),active_run_id:packet.run_id,environment:'connected-canary'}}}];`},id:crypto.randomUUID(),name:'Load Approved Realistic PB MT SN Packet',type:'n8n-nodes-base.code',typeVersion:2,position:[-280,0]},
 {parameters:{source:'database',workflowId:{__rl:true,value:childId,mode:'list',cachedResultName:'PRD Genie - Requirement Extractor Child v1.3'},workflowInputs:{mappingMode:'defineBelow',value:{},matchingColumns:[],schema:[],attemptToConvertTypes:false,convertFieldsToString:true},mode:'once',options:{waitForSubWorkflow:true}},id:crypto.randomUUID(),name:'Execute Requirement Extractor Child v1.3',type:'n8n-nodes-base.executeWorkflow',typeVersion:1.3,position:[0,0]},
 {parameters:{jsCode:`const stage=$input.first().json;const expected=${JSON.stringify(expected)};const errors=[];if(stage.stage!=='requirement_extraction'||stage.execution_status!=='passed'||stage.next_route!=='gap_analysis')errors.push('integration route');const actual=stage.output||{};if(actual.extraction_status!==expected.extraction_status)errors.push('extraction status');if((actual.items||[]).length!==expected.items.length)errors.push('item count');if((actual.contradictions||[]).length!==expected.contradictions.length)errors.push('contradiction count');if((actual.missing_information||[]).length!==expected.missing_information.length)errors.push('missing-information count');const byId=list=>Object.fromEntries((list||[]).map(x=>[x.id,x]));const refSet=record=>new Set((record.evidence||[]).map(e=>e.source_id+'|'+e.location+'|'+e.quote));const hasApprovedEvidence=(got,want)=>{const refs=refSet(got);return (want.evidence||[]).every(e=>refs.has(e.source_id+'|'+e.location+'|'+e.quote));};const ai=byId(actual.items),ei=byId(expected.items);for(const [id,want]of Object.entries(ei)){const got=ai[id];if(!got||got.type!==want.type||got.status!==want.status)errors.push('semantic parity '+id);else if(!hasApprovedEvidence(got,want))errors.push('traceability '+id);}if(Object.keys(ai).some(id=>!ei[id]))errors.push('unsupported item');const ac=byId(actual.contradictions),ec=byId(expected.contradictions);for(const [id,want]of Object.entries(ec)){const got=ac[id];if(!got||got.resolution_status!=='unresolved'||JSON.stringify([...(got.item_ids||[])].sort())!==JSON.stringify([...want.item_ids].sort()))errors.push('contradiction '+id);}if(Object.keys(ac).some(id=>!ec[id]))errors.push('unsupported contradiction');const am=byId(actual.missing_information),em=byId(expected.missing_information);for(const [id,want]of Object.entries(em)){const got=am[id];if(!got||got.category!==want.category||!hasApprovedEvidence(got,want))errors.push('missing '+id);}if(Object.keys(am).some(id=>!em[id]))errors.push('unsupported missing');if(stage.validation?.unsupported_claims!==0||stage.groundedness_percent!==100||stage.validation?.groundedness_percent!==100)errors.push('grounding');if(!stage.validation?.exact_source_traceability)errors.push('source traceability');if(errors.length)throw new Error('Realistic multi-source canary failed: '+[...new Set(errors)].join('; '));return [{json:{schema_version:'1.0.0',result_type:'realistic_multi_source_requirement_extraction_canary_result',run_id:stage.run_id,execution_status:'completed',contract_status:'passed',current_stage:'requirement_extraction',next_route:'gap_analysis',groundedness_percent:100,unsupported_claims:0,semantic_parity:true,exact_source_traceability:true,approved_baseline_sha256:'aec924acffa6359bba0e0d73fdc4e5774db3e56a12c7ecff352e74b51a8111b8',requirement_extraction_stage:stage,recorded_at:new Date().toISOString()}}];`},id:crypto.randomUUID(),name:'Validate Approved Realistic Ground Truth',type:'n8n-nodes-base.code',typeVersion:2,position:[280,0]}
 ],connections:{'Manual Trigger':{main:[[{node:'Load Approved Realistic PB MT SN Packet',type:'main',index:0}]]},'Load Approved Realistic PB MT SN Packet':{main:[[{node:'Execute Requirement Extractor Child v1.3',type:'main',index:0}]]},'Execute Requirement Extractor Child v1.3':{main:[[{node:'Validate Approved Realistic Ground Truth',type:'main',index:0}]]}},settings:{executionOrder:'v1'},active:false,versionId:crypto.randomUUID(),meta:{templateCredsSetupCompleted:true},tags:[]};
fs.writeFileSync('workflows/n8n/prd-genie-realistic-multi-source-canary-v0.2.json',JSON.stringify(parent,null,2)+'\n');
console.log('Built child v1.3 and realistic canary v0.2 for child ID '+childId);
