# Realistic Multi-Source Regression v1

This fixture preserves the three supplied capstone resources byte-for-byte:

- `product-brief.txt`
- `meeting-transcripts.txt`
- `stakeholder-notes.txt`

`source-packet.json` is generated by `scripts/build_realistic_multi_source_packet.mjs`. Its 70 reviewed citations identify the product-relevant statements that may support extraction; inclusion as a citation does not mean the statement is approved, non-contradictory, or sufficiently specific. The Requirement Extractor must still distinguish stated requirements, suggestions, open questions, vague fragments, alternatives, and contradictions.

This packet is not T1 and has `test_id: null`. It runs as the production-style multi-source route and must never be combined with `eval_prdgenie_inputs` in one run.

## Stakeholder clarification extension

The frozen three-source `source-packet.json` remains the accepted v1 evidence used by executions `9661` and `9667`. Stakeholder Vipin decided all 14 clarification gaps on 2026-08-07. Those later decisions are preserved without rewriting the original sources in:

- `stakeholder-clarifications-2026-08-07.md` — dated human-readable source;
- `stakeholder-clarification-decisions-2026-08-07.json` — canonical machine-readable decisions;
- `expected-clarification-resolution.json` — expected semantic resolution baseline; and
- `source-packet-v2.json` — canonical four-source packet `SP-REALISTIC-PB-MT-SN-CLAR-V2` for the next controlled run.

All four artifacts state or inherit `pending_n8n_verification`. They must not be described as runtime-resolved until new n8n Requirement Extraction and Gap Analysis executions pass.

Current acceptance status:

- byte/content-hash integrity: passed;
- source identity and exact line citations: passed;
- route separation: passed;
- input groundedness: **100%**;
- approved Requirement Extraction ground truth: approved on 2026-08-06;
- v1 Requirement Extraction and Gap Analysis evidence: executions `9661` and `9667` accepted;
- v2 clarification artifact: locally accepted, runtime rerun pending.

`expected-requirement-extraction.json` is the conservative, deterministic expected
Requirement Extractor output prepared from the approved citation review. It keeps
open questions out of the requirement list, records unresolved conflicts explicitly,
and preserves exact source evidence. It remains a human-review artifact until approved;
its approved SHA-256 and decision are recorded in `GROUND_TRUTH_APPROVAL.md`.

The dated clarification decision record and exact hashes are recorded in `CLARIFICATION_GROUND_TRUTH.md`.
